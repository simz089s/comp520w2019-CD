// Approximate the square root of x

var x: float = 0.0;
var guess: float = 1.0;
var iter: int = 10;

read(x);

while (iter) {
    var quot: float = x / guess;
    guess = 0.5 * (guess + quot);
    iter = iter - 1;
}

print(guess);
print(guess * guess);
